import { FileTypeValidator } from './filetypevalidator.directive';

describe('FiletypevalidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new FileTypeValidator();
    expect(directive).toBeTruthy();
  });
});
